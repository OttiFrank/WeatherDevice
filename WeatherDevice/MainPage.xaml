<Page
    x:Class="WeatherDevice.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeatherDevice"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="ScenarioControls" Visibility="Visible">
                Current Temperature: <Run x:Name="CurrentTemp"/>
                <LineBreak/>
                Current Humidity: <Run x:Name="CurrentHumidity"/>
                <LineBreak/>
                Current Wind Speed: <Run x:Name="CurrentWindSpeed"/>
                <LineBreak/>
                Last measured wind speed mean: <Run x:Name="CurrentMeanWind"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</Page>
